---
driver:
  name: vagrant
  provider:
    name: virtualbox
platforms:
  - name: nextcloud-buster64
    box: debian/buster64
lint: |
  set -e
  yamllint .
#  ansible-lint
provisioner:
  name: ansible
  side_effect: side_effect.yml
